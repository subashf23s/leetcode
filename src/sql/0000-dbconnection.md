To open psql connection in kali linux

`sudo -u postgres psql`

To check status

`sudo systemctl status postgresql`

To Start Service

`sudo systemctl start postgresql`

To Stop service

`sudo systemctl stop postgresql`
